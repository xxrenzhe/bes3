将 18 个视图彻底拆分为 18 个独立的 Stitch 指令，是**最高效、最不易出错，且最能保证每行代码质量**的终极方案。这样大模型就能把所有的 Token 算力倾注在单个页面的细节打磨和 Alpine.js 交互逻辑上。

为了确保这 18 次独立生成的页面在组装后**风格 100% 绝对统一**，我为每一个指令都植入了**完全相同**的 `Global Design System`（全局设计规范）头部。

请在 Stitch 的 **Code/Build（代码生成）模式**下，开启 18 个独立对话（或在同一个对话中逐次发送），依次复制以下完整的指令块：

---

### 1. Review Landing Page (Top 3 转化单页)

```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`) for long text.
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`), Inputs (`ring-2 ring-emerald-500` on focus). All buttons need `transition-all`.
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 1 - Review Landing Page (Top 3 Listicle)
Build the core SEO money page (e.g., "Top 3 Mac Keyboards").
- Structure: H1, Semantic Breadcrumbs, 3 Product Blocks (BLUF summary, Specs Table, Bes3 Radar Score, "Who is NOT for" warning box).
- CR & UI Strict Rules: 
  1. The main CTA button MUST say "Check Current Price on Amazon" (Do NOT use "Buy Now").
  2. Hardcode a Trust Badge directly near/under every main CTA: "✅ Hand-tested by Alex | Ad-free independent review" in small gray text.
  3. The "Who is NOT for" (Cons) section MUST use a highly visible warning UI (e.g., a subtle red or amber background/border) to build objective trust.
- UX/Logic (Alpine.js): 
  1. Mobile Sticky Bottom CTA: Use `@scroll.window` to show a `fixed bottom-0 left-0 w-full` "Check Price" bar ONLY when scrolling past the Hero section. Hide it slightly on quick scroll down, reveal on scroll up.
  2. "Copy Link" button: Click to show a "Copied!" tooltip for 2 seconds using `setTimeout`.
```

### 2. Head-to-Head Comparison (A vs B 终极对决页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`), Inputs (`ring-2 ring-emerald-500`). All buttons need `transition-all`.
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 2 - Head-to-Head Comparison (Product A vs Product B)
Build a high-conversion VS page.
- Structure: H1, Side-by-side layout comparing two products.
- UX/Logic (Tailwind/Alpine):
  1. The comparison table headers (Product Names/Images) MUST be `sticky top-0 z-10 bg-white` so they stay visible when scrolling down long spec rows.
  2. Include a distinct "Winner" badge for the winning product with a green glow/highlight.
```

### 3. Single Product Deep-Dive (单品深度评测)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`). All buttons need `transition-all`.
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 3 - Single Product Deep-Dive
Build an in-depth review page for a single specific product.
- Structure: Massive Hero image area, deep analysis sections, pros/cons list, and a prominent CTA area.
- CR & UI Strict Rules:
  1. The main call-to-action MUST be "Check Current Price on Amazon".
  2. Below the CTA, include the Trust Badge: "✅ Hand-tested by Alex | Ad-free independent review".
  3. The "Cons" list must be visually distinct, using red warning icons (X or alert icons) to establish neutrality.
- UX/Logic (Alpine.js):
  1. Implement an interactive Image Gallery (`x-data="{ activeImg: 'main.jpg' }"`). Show a main stage image, with a grid of 4 thumbnails below. Clicking a thumbnail updates the main stage image dynamically.
```

### 4. Informational / How-To Guide (科普指南页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 4 - Informational / How-To Guide
Build a pure educational SEO page (e.g., "Mechanical Switch Types Explained"). Zero hard-selling.
- Structure: Desktop layout needs a left sidebar and a wide text block on the right using `prose prose-emerald`.
- UX/Logic (Tailwind/Alpine):
  1. Implement a visually `sticky top-24` Table of Contents (TOC) in the sidebar.
  2. Add simple hover effects to internal links.
```

### 5. The Hub (Home Page / 聚合首页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 5 - The Hub (Home Page)
Build the high-speed spider-trap front page.
- Structure: Hero section ("The Best 3 Tech Picks, Decoded"), Directory Grid (3 columns for categories), and Trending Picks.
- UX/Logic (Tailwind):
  1. Implement smooth CSS hover effects on all Category Cards to lift them slightly (`hover:-translate-y-1 hover:shadow-md transition-all`).
```

### 6. Category Page (Topic Archive / 分类列表页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 6 - Category Page
Build a specific topic archive (e.g., "Mechanical Keyboards").
- Structure: Semantic Breadcrumbs, Category H1, a short SEO description paragraph, and a grid of 6 mock Article Cards.
- Detail: Each card must have an image placeholder, category badge, title, and "Read the Verdict" text link.
```

### 7. HTML Sitemap (The Spider Trap / 全站索引地图)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 7 - HTML Sitemap
Build a highly compact, text-heavy directory optimized for Google bots.
- Structure: H1 ("Bes3 Directory"). A multi-column text layout. List all categories as H2s, and nest bullet points of article titles (links) underneath.
- Focus: Extreme minimalism. No images. Fast rendering.
```

### 8. Deals & Sales Hub (大促折扣中心)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Cards (`bg-white shadow-sm border border-gray-200 rounded-xl`).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 8 - Deals & Sales Hub
Build a high-urgency page for Prime Day / Black Friday.
- Structure: Grid of deal cards with red "Live Deal" badges, struck-through original prices, and prominent discount % badges.
- UX/Logic (Alpine.js):
  1. Add a simulated "Countdown Timer" UI at the top (e.g., "Sale ends in 04:59:59"). Use Alpine to tick down the seconds dynamically.
```

### 9. Newsletter Subscribe (邮件私域订阅页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Inputs (`ring-2 ring-emerald-500` on focus).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 9 - Newsletter Subscribe
Build a high-conversion lead magnet page.
- Structure: Split layout. Left side: Value proposition. Right side: Clean email capture form inside a card.
- UX/Logic (Alpine.js):
  1. Form validation: Disable the "Subscribe" button until the email input contains an "@" symbol.
  2. Success State: On submit, use `x-transition` to hide the form and show a green "Thanks for joining!" checkmark state.
```

### 10. About Us & How We Test (E-E-A-T 核心页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 10 - About Us & How We Test
Build the ultimate trust-building page for Google Quality Raters.
- Structure: H1 ("About Bes3 & Our Methodology").
- Sections: 
  1. "Our 3-Step Testing Process" (Visual 3-column layout with icons for Scrape, Analyze, Verdict).
  2. "Why Only 3 Picks?" (A text manifesto on decision fatigue).
```

### 12. Standard Legal Template (通用合规页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 12 - Standard Legal Template
Build a reusable shell for Privacy Policy, TOS, and Affiliate Disclosure.
- Structure: A highly readable, centered narrow column layout (`max-w-3xl mx-auto`).
- UX/Logic: Use the `prose prose-slate` class to automatically format mock H2s, paragraphs, and bullet lists elegantly. Include a "Last Updated: [Date]" dynamic-looking text at the top.
```

### 13. Contact Us (联系我们)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Components: Inputs (`ring-2 ring-emerald-500` on focus).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 13 - Contact Us
Build a standard contact page.
- Structure: Two columns. Left: "Get in Touch" info and email address. Right: Clean Contact Form.
- UX/Logic (Alpine.js): Simulate form submission. When clicking "Send", change button text to "Sending..." with a simulated loading state for 1 second, then replace the form with a Success Message.
```

### 14. Search Results & Empty State (搜索与兜底页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 14 - Search Results & Empty State
Build an interactive search experience.
- Structure: Big search input at the top. Results grid below.
- UX/Logic (Alpine.js): Bind input to `x-model="searchQuery"`.
  - If `searchQuery` is empty: Show "Trending Picks".
  - If `searchQuery` equals "keyboard": Show 3 mock result cards.
  - If `searchQuery` equals anything else: Show a friendly "No results found" Empty State with an illustration/icon.
```

### 15. 404 Error Page (404 错误页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 15 - 404 Error Page
Build a traffic-saver error page.
- Structure: Prominent "404 - Oops! Page not found" message.
- Essential UX: Below the error, include a "Try our Trending Picks" section with 3 article cards to prevent the user from bouncing off the site completely. Provide a "Back to Home" primary button.
```

### 16. Thank You / Success Page (成功回调页)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600` (hover `emerald-700`). Background `gray-50`. Text `gray-900` (headings), `gray-600` (body).
- Layout: Include a standard "Bes3" Navbar and Footer.

# Task: View 16 - Thank You / Success Page
Build a post-action conversion page (used after forms or specific flows).
- Structure: Large success icon (check circle), "Message Sent! / Thank You" H1.
- Essential UX: Do not let it be a dead end. Include a big "Return to Homepage" button and a grid of "Top Trending Reviews" to keep the user in the funnel.
```

### 17. Local Admin Dashboard (工作流中台 UI)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600`. **Background `gray-100` (content), `gray-900` (sidebar).** Text `gray-900` (headings).

# Task: View 17 - Local Admin Dashboard (Workflows)
Build the internal tool used to generate the static site. (Do NOT include the public Navbar/Footer).
- Structure: Split screen. Left dark sidebar (`w-64 bg-gray-900`). Right light content area (`bg-gray-100`).
- UX/Logic (Alpine.js Tabs): Implement 3 functional tabs in the main area tailored for a pure Python pipeline:
  1. **Miner Tab**: An input field for "Seed Keyword" and a "Run Autocomplete Miner" button.
  2. **Batch Generator Tab**: A drag-and-drop zone for "Upload CSV (Keyword + ASIN)". Include a "Start Async Batch" button. On click, show a simulated Alpine.js progress bar (0% to 100%) and a terminal-like log box appending "Generating JSON-LD...", "Rendering HTML...".
  3. **GitOps & Indexing Tab**: Dead link/Price shift alert table. A massive "Deploy & Ping Google Indexing API" button that opens an Alpine Modal confirming the action.
```

### 18. Admin Settings & Config (管理后台全局配置)
```text
# Role & Global Design System
You are a Principal UI/UX Engineer coding a production-ready template for "Bes3" (bes3.com). Output functional HTML with Tailwind CSS and Alpine.js via CDN.
- Typography: Inter-like sans-serif. Use `@tailwindcss/typography` (`prose`).
- Colors: Primary `emerald-600`. **Background `gray-100` (content), `gray-900` (sidebar).** Text `gray-900` (headings).

# Task: View 18 - Admin Settings & Config
Build the configuration panel for the local generator. (Do NOT include the public Navbar/Footer).
- Structure: Split screen. Left dark sidebar, right light content area.
- UX/Logic (Alpine.js): 
  1. Build a form card with inputs for: Gemini API Key, Playwright Proxy, GitHub Token.
  2. Password Visibility: Add an eye icon to API key fields to toggle `type="password"` and `type="text"`.
  3. Form State: If any input changes, show a yellow warning badge "Unsaved Changes" near the "Save Settings" button.
```